# Hodos graph

A complete Hara Play project whose `workspace.edn` declares a `hodos.2d/graph` area directly.

The graph contains stable node, port and connection identities. Play admits only selection and node movement in this first consumer slice. Hodos owns safe DOM/SVG mechanics; Play owns canonical graph mutation.

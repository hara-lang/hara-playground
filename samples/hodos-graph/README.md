# Hodos graph

A complete Hara Playground project whose `workspace.edn` declares a `hodos.2d/graph` area directly.

The graph contains stable node, port and connection identities. Playground admits only selection and node movement in this first consumer slice. Hodos owns safe DOM/SVG mechanics; Playground owns canonical graph mutation.

# Live values

Open `src/main.hal`, change the order values, and leave the cursor inside a complete form. InstaREPL evaluates it through the persistent Hara kernel. Run `(view)` to refresh the HTA preview.

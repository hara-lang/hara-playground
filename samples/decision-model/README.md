# Decision model

Change the values in `signals`, inspect `score` beside the form, and watch the branch result change. The REPL trace and preview are both produced by the same persistent Hara kernel session.
